function square (num) // This is the typical function way
{
    return num * num;
}

const square = (num) => // The arrow function and its recommendation it's to use it with "const"
{
    return num * num;
}

const square = num => num * num; // Same arrow function but more compact
